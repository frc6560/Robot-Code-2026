# BLine paths

Export BLine path JSON files into this directory. A file named `example.json` is loaded in Java with:

```java
new Path("example")
```

Do not include the `.json` extension in the `Path` constructor.
